{
  "name": "telegram-worker",
  "version": "1.0.0",
  "description": "HOOX notification worker: Telegram Bot API alerts, RAG copilot (/ask, /search), Vectorize + Workers AI, fan-in from the mesh.",
  "license": "Apache-2.0",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hoox-sh/hoox-shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3",
    "wrangler": "^4.95.0"
  },
  "author": "jango-blockchained (hoox-sh)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hoox-sh/telegram-worker.git"
  }
}
